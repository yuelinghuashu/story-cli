# .storyignore — 排除不想被 story-cli 扫描的目录/文件
# 语法：.gitignore 简化子集（不支持 ! 取反、** 递归、/ 锚定）
#
# 支持的语法：
#   _draft/      # 排除整个目录（/ 结尾表示仅目录）
#   *.tmp        # 通配符匹配（* 不跨越目录分隔符）
#   temp.md      # 排除特定文件
#
# 注意：被排除的内容仍可被 Git 跟踪（放入此文件控制的是 story-cli
# 的扫描/统计/README 生成/EPUB 导出，不影响版本控制）。

# 草稿与构思（放入 Git 备份，但不参与故事库）
_draft/
_notes/
idea-*.md

# 临时文件
*~
*.tmp